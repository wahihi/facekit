name: facekit
description: "Flutter on-device face recognition SDK. Clean-room implementation using public models and papers."
version: 0.1.0
homepage: https://github.com/facekit-dev/facekit

environment:
  sdk: ^3.12.2
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.24.0

flutter:
  assets:
    - assets/models/blazeface_short/
